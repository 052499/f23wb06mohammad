extends layout

block content
  h1= title
  
  - var image_names=["img-1.jpg","img-2.jpg","img-3.jpg","img-4.jpg","img-5.jpg"]
  - for(let i=0;i<3;i++){
     - let randomNumber=Math.floor(Math.random()*5)
     img(src=/images/${image_names[randomNumber]} alt=${randomNumber} height="300px" width="300px")
    -}